export default function Settings() {
  return (
    <div>
      <h2 className="text-2xl font-semibold mb-1">Settings</h2>
      <p className="text-soft text-sm">
        AI tier selection (Groq / your API key / CLI bridge) arrives with Phases 1 and 5.
      </p>
    </div>
  );
}
